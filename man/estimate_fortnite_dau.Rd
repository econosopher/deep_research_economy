% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{estimate_fortnite_dau}
\alias{estimate_fortnite_dau}
\title{Estimate Fortnite DAU from a random island sample}
\usage{
estimate_fortnite_dau(
  date = Sys.Date() - 1,
  sample_size = 100,
  confidence_level = getOption("fortniteR.confidence_level", 0.95),
  max_pages = 20,
  page_size = 100,
  overlap_adjustment = 1,
  na_as_zero = TRUE,
  seed = NULL,
  quiet = FALSE
)
}
\arguments{
\item{date}{Date to estimate DAU for. Defaults to yesterday.}

\item{sample_size}{Number of islands to sample from the frame.}

\item{confidence_level}{Confidence level for the interval (e.g., 0.9, 0.95, 0.99).
Defaults to `getOption("fortniteR.confidence_level", 0.95)`.}

\item{max_pages}{Maximum number of pages to pull for the sampling frame.}

\item{page_size}{Number of islands per page for the sampling frame.}

\item{overlap_adjustment}{Divide the island-level estimate by this value to
approximate platform-level DAU. Set to 1 for no adjustment.}

\item{na_as_zero}{If `TRUE`, missing/null `uniquePlayers` are treated as 0.}

\item{seed}{Optional random seed for reproducibility.}

\item{quiet}{If `FALSE`, prints progress messages.}
}
\value{
A list with:
\describe{
  \item{summary}{A one-row tibble with point estimate and confidence bounds.}
  \item{sample}{A tibble with sampled islands and per-island values used.}
}
}
\description{
This function estimates daily active users (DAU) using a random sample of
islands from the Fortnite Data API. The estimate is based on island-level
`uniquePlayers` and includes a confidence interval that can be tuned with
`confidence_level`.
}
\details{
Important: this is an estimator, not an official platform DAU metric.
Island-level unique players are not de-duplicated across islands. Use
`overlap_adjustment` (> 1) to approximate cross-island player overlap when
converting to a platform-level DAU estimate.
}
\examples{
\dontrun{
est <- estimate_fortnite_dau(
  date = Sys.Date() - 1,
  sample_size = 100,
  confidence_level = getOption("fortniteR.confidence_level", 0.95),
  overlap_adjustment = 1.4,
  seed = 42
)

est$summary
}
}
