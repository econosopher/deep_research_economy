% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{estimate_fortnite_timeseries}
\alias{estimate_fortnite_timeseries}
\title{Estimate sampled Fortnite metrics over time}
\usage{
estimate_fortnite_timeseries(
  start_date = Sys.Date() - 6,
  end_date = Sys.Date() - 1,
  metrics = c("unique_players"),
  sample_size = 100,
  confidence_level = getOption("FortniteR.confidence_level", 0.95),
  max_pages = 20,
  page_size = 100,
  overlap_adjustment = 1,
  na_as_zero = TRUE,
  seed = NULL,
  quiet = FALSE
)
}
\arguments{
\item{start_date}{Start date (inclusive). Defaults to 6 days ago.}

\item{end_date}{End date (inclusive). Defaults to yesterday.}

\item{metrics}{Character vector of metrics to estimate. Supported values:
`"unique_players"`, `"plays"`, `"peak_ccu"`, `"minutes_played"`,
`"average_play_time_seconds"`, `"favorites"`, `"recommendations"`,
`"retention_1_day"`, `"retention_7_days"`.}

\item{sample_size}{Number of islands to sample from the frame.}

\item{confidence_level}{Confidence level for intervals. Defaults to
`getOption("FortniteR.confidence_level", 0.95)`.}

\item{max_pages}{Maximum number of pages to pull for the sampling frame.}

\item{page_size}{Number of islands per page for the sampling frame.}

\item{overlap_adjustment}{Divide total estimates by this value to produce
adjusted estimates (for DAU de-dup approximation, for example).}

\item{na_as_zero}{If `TRUE`, missing/null metric values are treated as 0.}

\item{seed}{Optional random seed for reproducibility.}

\item{quiet}{If `FALSE`, prints progress messages.}
}
\value{
A list with:
\describe{
  \item{summary}{A tibble with one row per `date` and `metric`.}
  \item{sample}{A tibble of sampled islands by date with raw metric values.}
}
}
\description{
Estimates one or more metrics over a daily time series using a single random
island sample. This reduces API stress versus re-sampling each day because
each sampled island is queried once for the whole date range.
}
\details{
For `metric = "unique_players"`, this can be interpreted as a DAU time series
estimate (after optional `overlap_adjustment`).
}
\examples{
\dontrun{
ts_est <- estimate_fortnite_timeseries(
  start_date = Sys.Date() - 6,
  end_date = Sys.Date() - 1,
  metrics = c("unique_players", "plays"),
  sample_size = 150,
  confidence_level = 0.95,
  overlap_adjustment = 1.4,
  seed = 42
)

ts_est$summary
}
}
